import { db } from "./db";
import { lessons, challenges, communityPosts } from "@shared/schema";

export async function seedDatabase() {
  // Only seed if fewer than 80 lessons exist
  const existing = await db.select().from(lessons).limit(1);
  const count = await db.select().from(lessons);
  if (count.length >= 80) return;

  console.log("🌱 Seeding database with default lessons and challenges...");

  await db.insert(lessons).values([
    // ── BEGINNER (20 lessons) ──────────────────────────────────────────
    { id: "1", title: "What is Programming?", description: "Introduction to programming concepts and JavaScript", content: "Programming is the process of creating instructions for computers to follow. JavaScript is a popular programming language that runs in web browsers and servers. It's used to create interactive websites, mobile apps, and server applications.\n\nKey concepts:\n• Algorithms - step-by-step instructions\n• Syntax - the rules of the language\n• Variables - containers for data\n• Functions - reusable blocks of code\n\nJavaScript is beginner-friendly, widely used, and has a large community for support.", codeExample: "// Your first JavaScript program\nconsole.log('Hello, World!');\nconsole.log('Welcome to programming!');", difficulty: "beginner", category: "fundamentals", order: 1, isCompleted: false },
    { id: "2", title: "Setting Up Your Environment", description: "Understanding the development environment and browser console", content: "Before writing code, you need to understand where JavaScript runs:\n\n• Browser Console - press F12 or right-click → Inspect\n• Code Editors - VS Code, Atom, or online editors\n• Running code - in browser console or HTML file\n\nThe browser console is perfect for practicing JavaScript.", codeExample: "// Try these in your browser console\nconsole.log('Testing my setup');\nconsole.log(2 + 3);\nconsole.log('Current time:', new Date());", difficulty: "beginner", category: "fundamentals", order: 2, isCompleted: false },
    { id: "3", title: "Variables and Data Storage", description: "Learning to store and manage data with variables", content: "Variables are containers that store data values. Think of them as labeled boxes where you can put different types of information.\n\nThree ways to declare variables:\n• let - for values that can change\n• const - for values that stay the same\n• var - older way (avoid in modern code)\n\nNaming rules:\n• Start with letter, underscore, or $\n• Can contain letters, numbers, underscore, $\n• Case sensitive (name ≠ Name)\n• Use descriptive names", codeExample: "// Declaring variables\nlet userName = 'Alice';\nconst userAge = 25;\nlet isLoggedIn = true;\n\n// Using variables\nconsole.log('User:', userName);\nconsole.log('Age:', userAge);\nconsole.log('Logged in:', isLoggedIn);", difficulty: "beginner", category: "fundamentals", order: 3, isCompleted: false },
    { id: "4", title: "Data Types Explained", description: "Understanding different types of data in JavaScript", content: "JavaScript has several data types:\n\nPrimitive types:\n• Number - integers and decimals (42, 3.14)\n• String - text in quotes\n• Boolean - true or false\n• Undefined - declared but no value\n• Null - intentionally empty\n\nUse typeof to check data types.", codeExample: "let age = 25;\nlet name = 'John';\nlet isStudent = true;\nlet address;\nlet middleName = null;\n\nconsole.log(typeof age);\nconsole.log(typeof name);\nconsole.log(typeof isStudent);", difficulty: "beginner", category: "fundamentals", order: 4, isCompleted: false },
    { id: "5", title: "Working with Numbers", description: "Mathematical operations and number manipulation", content: "Numbers in JavaScript can be integers or floating-point. Arithmetic operators:\n• + addition\n• - subtraction\n• * multiplication\n• / division\n• % modulus\n• ** exponentiation", codeExample: "let a = 10;\nlet b = 3;\nconsole.log(a + b);\nconsole.log(a - b);\nconsole.log(a * b);\nconsole.log(a / b);\nconsole.log(a % b);\nconsole.log(Math.round(3.7));\nconsole.log(Math.max(5, 10));", difficulty: "beginner", category: "fundamentals", order: 5, isCompleted: false },
    { id: "6", title: "Working with Strings", description: "Text manipulation and string operations", content: "Strings represent text. Create them with single quotes, double quotes, or backticks.\n\nCommon operations:\n• Concatenation - joining strings\n• Length - counting characters\n• Template literals - embedding variables", codeExample: "let firstName = 'John';\nlet lastName = 'Doe';\nlet greeting = `Hello, ${firstName} ${lastName}!`;\nconsole.log(greeting);\nconsole.log(firstName.length);\nconsole.log(firstName.toUpperCase());", difficulty: "beginner", category: "fundamentals", order: 6, isCompleted: false },
    { id: "7", title: "Conditional Statements", description: "Making decisions in your code with if/else statements", content: "Conditional statements let your program make decisions.\n\nComparison operators:\n• === equal\n• !== not equal\n• > greater than\n• < less than\n\nLogical operators:\n• && and\n• || or\n• ! not", codeExample: "let age = 18;\nlet hasLicense = true;\n\nif (age >= 18 && hasLicense) {\n  console.log('Can drive!');\n} else if (age >= 18) {\n  console.log('Need a license');\n} else {\n  console.log('Too young');\n}", difficulty: "beginner", category: "fundamentals", order: 7, isCompleted: false },
    { id: "8", title: "Introduction to Functions", description: "Creating reusable blocks of code with functions", content: "Functions are reusable blocks of code.\n\nComponents:\n• Function name\n• Parameters - input values\n• Function body\n• Return value (optional)", codeExample: "function greetUser(name, age) {\n  return `Hello ${name}, you are ${age} years old!`;\n}\n\nlet message = greetUser('Alice', 25);\nconsole.log(message);", difficulty: "beginner", category: "fundamentals", order: 8, isCompleted: false },
    { id: "9", title: "Function Parameters and Return", description: "Understanding function inputs and outputs", content: "Functions accept inputs (parameters) and provide outputs (return values).\n\nKey concepts:\n• Parameters vs Arguments\n• Return statement\n• Default parameters\n• Scope", codeExample: "function calculateArea(width, height) {\n  return width * height;\n}\n\nfunction greet(name = 'Guest') {\n  return `Hello, ${name}!`;\n}\n\nconsole.log(calculateArea(5, 3));\nconsole.log(greet());\nconsole.log(greet('Alice'));", difficulty: "beginner", category: "fundamentals", order: 9, isCompleted: false },
    { id: "10", title: "Introduction to Arrays", description: "Storing multiple values in ordered lists", content: "Arrays are ordered lists storing multiple values.\n\nCharacteristics:\n• Zero-indexed - first item at position 0\n• Dynamic - can grow and shrink\n• Mixed types allowed", codeExample: "let fruits = ['apple', 'banana', 'orange'];\nconsole.log(fruits[0]);\nconsole.log(fruits.length);\nfruits.push('grape');\nconsole.log(fruits);", difficulty: "beginner", category: "fundamentals", order: 10, isCompleted: false },
    { id: "11", title: "Array Methods", description: "Adding, removing, and modifying array elements", content: "Common array methods:\n• push() - add to end\n• pop() - remove from end\n• unshift() - add to beginning\n• shift() - remove from beginning\n• splice() - add/remove at position\n• slice() - copy portion", codeExample: "let fruits = ['apple', 'banana'];\nfruits.push('orange');\nfruits.unshift('grape');\nconsole.log(fruits);\nlet last = fruits.pop();\nconsole.log(last);", difficulty: "beginner", category: "fundamentals", order: 11, isCompleted: false },
    { id: "12", title: "Loops - Repeating Actions", description: "Using loops to repeat code efficiently", content: "Loops repeat code multiple times:\n• for loop - known count\n• while loop - condition based\n• for...of - iterate arrays\n• for...in - iterate objects", codeExample: "for (let i = 1; i <= 5; i++) {\n  console.log('Count:', i);\n}\n\nlet fruits = ['apple', 'banana', 'orange'];\nfor (let fruit of fruits) {\n  console.log('Fruit:', fruit);\n}", difficulty: "beginner", category: "fundamentals", order: 12, isCompleted: false },
    { id: "13", title: "Introduction to Objects", description: "Storing related data with properties and methods", content: "Objects are key-value pairs representing entities.\n\nCharacteristics:\n• Properties - stored data\n• Methods - object functions\n• Dot notation - object.property\n• Bracket notation - object['property']", codeExample: "let person = {\n  name: 'Alice',\n  age: 25,\n  city: 'New York'\n};\n\nconsole.log(person.name);\nconsole.log(person['age']);\nperson.email = 'alice@email.com';\nconsole.log(person);", difficulty: "beginner", category: "fundamentals", order: 13, isCompleted: false },
    { id: "14", title: "Object Methods and this", description: "Adding functions to objects and using this keyword", content: "Objects can have methods (functions). The 'this' keyword refers to the current object.\n\nKey concepts:\n• Method definition\n• Using this inside methods\n• Object destructuring", codeExample: "let person = {\n  name: 'Alice',\n  age: 25,\n  greet() {\n    return `Hi, I'm ${this.name} and I'm ${this.age} years old`;\n  }\n};\n\nconsole.log(person.greet());", difficulty: "beginner", category: "fundamentals", order: 14, isCompleted: false },
    { id: "b15", title: "Switch Statements", description: "Handling multiple conditions cleanly with switch", content: "Switch statements are cleaner than long if/else chains when checking one value against many options.\n\nSyntax:\n• switch(value) — the value to test\n• case — each option to match\n• break — stop after a match\n• default — runs if nothing matches", codeExample: "let day = 'Monday';\n\nswitch (day) {\n  case 'Monday':\n    console.log('Start of work week');\n    break;\n  case 'Friday':\n    console.log('Almost weekend!');\n    break;\n  case 'Saturday':\n  case 'Sunday':\n    console.log('Weekend!');\n    break;\n  default:\n    console.log('Midweek');\n}", difficulty: "beginner", category: "fundamentals", order: 15, isCompleted: false },
    { id: "b16", title: "The Ternary Operator", description: "Writing concise conditional expressions", content: "The ternary operator is a short way to write a simple if/else.\n\nSyntax: condition ? valueIfTrue : valueIfFalse\n\nWhen to use:\n• Simple true/false assignments\n• Short inline conditions\n• Avoid for complex logic", codeExample: "let age = 20;\nlet status = age >= 18 ? 'adult' : 'minor';\nconsole.log(status);\n\n// Equivalent if/else:\nlet label;\nif (age >= 18) {\n  label = 'adult';\n} else {\n  label = 'minor';\n}\nconsole.log(label);\n\n// Inside template literals\nconsole.log(`User is ${age >= 18 ? 'old' : 'young'} enough.`);", difficulty: "beginner", category: "fundamentals", order: 16, isCompleted: false },
    { id: "b17", title: "String Methods", description: "Built-in methods to search, modify, and format strings", content: "JavaScript strings come with many useful built-in methods.\n\nKey methods:\n• toUpperCase() / toLowerCase() — change case\n• trim() — remove whitespace\n• includes() — check for substring\n• indexOf() — find position\n• replace() — swap text\n• split() — turn string into array", codeExample: "let sentence = '  Hello, JavaScript World!  ';\n\nconsole.log(sentence.trim());\nconsole.log(sentence.trim().toLowerCase());\nconsole.log(sentence.includes('JavaScript'));\nconsole.log(sentence.replace('World', 'Universe'));\n\nlet csv = 'apple,banana,orange';\nlet fruits = csv.split(',');\nconsole.log(fruits);", difficulty: "beginner", category: "fundamentals", order: 17, isCompleted: false },
    { id: "b18", title: "forEach and map", description: "Iterating and transforming arrays with higher-order functions", content: "Higher-order functions accept other functions as arguments.\n\n• forEach() — run code for each item (no return value)\n• map() — transform every item and return a new array\n\nThese replace many traditional for loops and make code more readable.", codeExample: "let numbers = [1, 2, 3, 4, 5];\n\n// forEach — just loop\nnumbers.forEach(n => console.log('Number:', n));\n\n// map — create new array\nlet doubled = numbers.map(n => n * 2);\nconsole.log(doubled); // [2, 4, 6, 8, 10]\n\nlet names = ['alice', 'bob', 'carol'];\nlet capitalized = names.map(name => name[0].toUpperCase() + name.slice(1));\nconsole.log(capitalized);", difficulty: "beginner", category: "fundamentals", order: 18, isCompleted: false },
    { id: "b19", title: "filter and reduce", description: "Selecting and combining array elements", content: "Two more essential array methods:\n\n• filter() — keeps only items that pass a test, returns new array\n• reduce() — combines all items into a single value (sum, product, object, etc.)\n\nThese are the foundation of functional programming in JavaScript.", codeExample: "let scores = [85, 42, 93, 67, 78, 55, 91];\n\n// filter — only passing scores\nlet passing = scores.filter(s => s >= 60);\nconsole.log(passing); // [85, 93, 67, 78, 91]\n\n// reduce — calculate average\nlet total = scores.reduce((sum, s) => sum + s, 0);\nlet average = total / scores.length;\nconsole.log('Average:', average.toFixed(1));", difficulty: "beginner", category: "fundamentals", order: 19, isCompleted: false },
    { id: "b20", title: "Scope and Variable Visibility", description: "Understanding where variables can be accessed", content: "Scope determines where a variable is accessible in your code.\n\nTypes of scope:\n• Global scope — accessible everywhere\n• Function scope — only inside a function\n• Block scope — only inside { } (let and const)\n\nUnderstanding scope prevents bugs caused by accidentally overwriting variables.", codeExample: "let globalVar = 'I am global';\n\nfunction myFunction() {\n  let localVar = 'I am local';\n  console.log(globalVar); // accessible\n  console.log(localVar);  // accessible\n}\n\nmyFunction();\nconsole.log(globalVar); // accessible\n// console.log(localVar); // ERROR — not accessible\n\n// Block scope\nif (true) {\n  let blockVar = 'block only';\n  console.log(blockVar);\n}\n// console.log(blockVar); // ERROR", difficulty: "beginner", category: "fundamentals", order: 20, isCompleted: false },

    // ── INTERMEDIATE (20 lessons) ──────────────────────────────────────
    { id: "15", title: "ES6 Arrow Functions", description: "Modern function syntax and features", content: "Arrow functions provide a concise syntax:\n\nTraditional: function(x) { return x * 2; }\nArrow: (x) => x * 2\n\nKey differences:\n• Shorter syntax\n• No own 'this' binding\n• Great for callbacks", codeExample: "// Traditional\nfunction double(x) { return x * 2; }\n\n// Arrow function\nconst doubleArrow = (x) => x * 2;\n\n// With arrays\nconst numbers = [1, 2, 3, 4, 5];\nconst doubled = numbers.map(n => n * 2);\nconsole.log(doubled);", difficulty: "intermediate", category: "es6", order: 21, isCompleted: false },
    { id: "16", title: "Destructuring", description: "Extracting values from arrays and objects", content: "Destructuring extracts values concisely:\n\nArray destructuring:\nconst [a, b] = [1, 2];\n\nObject destructuring:\nconst { name, age } = person;", codeExample: "// Array destructuring\nconst [first, second, ...rest] = [1, 2, 3, 4, 5];\nconsole.log(first, second, rest);\n\n// Object destructuring\nconst { name, age, city = 'Unknown' } = { name: 'Alice', age: 25 };\nconsole.log(name, age, city);", difficulty: "intermediate", category: "es6", order: 22, isCompleted: false },
    { id: "17", title: "Spread and Rest Operators", description: "Working with ... operator in JavaScript", content: "The spread operator (...) expands items.\nThe rest parameter (...) collects items.\n\nUses:\n• Combine arrays\n• Copy objects\n• Flexible function parameters", codeExample: "// Spread\nconst arr1 = [1, 2, 3];\nconst arr2 = [4, 5, 6];\nconst combined = [...arr1, ...arr2];\nconsole.log(combined);\n\n// Rest\nfunction sum(...numbers) {\n  return numbers.reduce((total, n) => total + n, 0);\n}\nconsole.log(sum(1, 2, 3, 4, 5));", difficulty: "intermediate", category: "es6", order: 23, isCompleted: false },
    { id: "18", title: "Promises and Async/Await", description: "Handling asynchronous operations in JavaScript", content: "Promises handle async operations:\n• pending - initial state\n• fulfilled - success\n• rejected - failure\n\nasync/await makes promises easier to read.", codeExample: "// Promise\nfunction fetchData() {\n  return new Promise((resolve, reject) => {\n    setTimeout(() => resolve('Data loaded!'), 1000);\n  });\n}\n\n// async/await\nasync function loadData() {\n  const data = await fetchData();\n  console.log(data);\n}\n\nloadData();", difficulty: "intermediate", category: "async", order: 24, isCompleted: false },
    { id: "19", title: "Error Handling", description: "Using try/catch to handle errors gracefully", content: "Error handling prevents crashes:\n• try - code that might fail\n• catch - handle the error\n• finally - always runs\n• throw - create errors", codeExample: "async function fetchUser(id) {\n  try {\n    const response = await fetch(`/api/users/${id}`);\n    if (!response.ok) throw new Error('User not found');\n    return await response.json();\n  } catch (error) {\n    console.error('Error:', error.message);\n    return null;\n  } finally {\n    console.log('Request completed');\n  }\n}", difficulty: "intermediate", category: "error-handling", order: 25, isCompleted: false },
    { id: "20", title: "Classes and OOP", description: "Object-oriented programming with ES6 classes", content: "Classes provide a clean OOP syntax:\n• constructor - initialize objects\n• methods - define behavior\n• extends - inheritance\n• super - call parent", codeExample: "class Animal {\n  constructor(name, sound) {\n    this.name = name;\n    this.sound = sound;\n  }\n  speak() {\n    return `${this.name} says ${this.sound}`;\n  }\n}\n\nclass Dog extends Animal {\n  constructor(name) {\n    super(name, 'woof');\n  }\n  fetch() { return `${this.name} fetches the ball!`; }\n}\n\nconst dog = new Dog('Rex');\nconsole.log(dog.speak());\nconsole.log(dog.fetch());", difficulty: "intermediate", category: "oop", order: 26, isCompleted: false },
    { id: "i7", title: "Modules and Imports", description: "Organizing code with ES6 modules", content: "Modules let you split code across files and share only what is needed.\n\nKey keywords:\n• export — make a value available to other files\n• export default — one main export per file\n• import — bring in values from other files\n\nBenefits: code reuse, easier maintenance, clear dependencies.", codeExample: "// math.js\nexport function add(a, b) { return a + b; }\nexport function multiply(a, b) { return a * b; }\nexport const PI = 3.14159;\n\n// main.js\nimport { add, multiply, PI } from './math.js';\nconsole.log(add(2, 3));       // 5\nconsole.log(multiply(4, 5));  // 20\nconsole.log(PI);              // 3.14159\n\n// Import everything\nimport * as math from './math.js';\nconsole.log(math.add(1, 1));", difficulty: "intermediate", category: "es6", order: 27, isCompleted: false },
    { id: "i8", title: "Map and Set", description: "Powerful built-in data structures", content: "Map and Set are modern alternatives to plain objects and arrays.\n\nMap:\n• Stores key-value pairs (any type as key)\n• Maintains insertion order\n• size property instead of length\n\nSet:\n• Stores unique values only\n• Great for removing duplicates\n• Fast lookup", codeExample: "// Map\nconst scores = new Map();\nscores.set('Alice', 95);\nscores.set('Bob', 87);\nscores.set('Carol', 92);\nconsole.log(scores.get('Alice')); // 95\nconsole.log(scores.size);         // 3\n\nfor (const [name, score] of scores) {\n  console.log(`${name}: ${score}`);\n}\n\n// Set — remove duplicates\nconst nums = [1, 2, 2, 3, 3, 4];\nconst unique = [...new Set(nums)];\nconsole.log(unique); // [1, 2, 3, 4]", difficulty: "intermediate", category: "data-structures", order: 28, isCompleted: false },
    { id: "i9", title: "Regular Expressions", description: "Pattern matching and text searching", content: "Regular expressions (regex) are patterns used to match text.\n\nCreating regex:\n• Literal: /pattern/flags\n• Constructor: new RegExp('pattern')\n\nCommon flags:\n• g — global (all matches)\n• i — case insensitive\n• m — multiline\n\nCommon patterns:\n• \\d — digit, \\w — word char, \\s — whitespace\n• + — one or more, * — zero or more, ? — optional", codeExample: "let email = 'user@example.com';\nlet emailRegex = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/;\nconsole.log(emailRegex.test(email)); // true\n\nlet text = 'The price is $25 and $40';\nlet prices = text.match(/\\$\\d+/g);\nconsole.log(prices); // ['$25', '$40']\n\nlet phone = '123-456-7890';\nlet formatted = phone.replace(/-/g, ' ');\nconsole.log(formatted); // 123 456 7890", difficulty: "intermediate", category: "strings", order: 29, isCompleted: false },
    { id: "i10", title: "DOM Manipulation", description: "Selecting and modifying HTML elements with JavaScript", content: "The DOM (Document Object Model) represents your webpage as a tree of objects you can manipulate.\n\nSelecting elements:\n• getElementById — by ID\n• querySelector — CSS selector\n• querySelectorAll — all matching\n\nModifying:\n• innerHTML / textContent — change content\n• style — change CSS\n• classList — add/remove classes\n• setAttribute — change attributes", codeExample: "// Select elements\nconst heading = document.querySelector('h1');\nconst buttons = document.querySelectorAll('button');\n\n// Modify content\nheading.textContent = 'Hello from JavaScript!';\nheading.style.color = 'blue';\n\n// Toggle a class\nheading.classList.toggle('highlight');\n\n// Create new element\nconst newPara = document.createElement('p');\nnewPara.textContent = 'Added dynamically!';\ndocument.body.appendChild(newPara);", difficulty: "intermediate", category: "dom", order: 30, isCompleted: false },
    { id: "i11", title: "Events and Event Listeners", description: "Responding to user interactions", content: "Events fire when users interact with your page (clicks, typing, hovering, etc.).\n\nAdding listeners:\n• addEventListener(event, handler)\n\nCommon events:\n• click, dblclick\n• keydown, keyup, keypress\n• mouseover, mouseout\n• submit, change, input\n\nEvent object:\n• event.target — element that triggered it\n• event.preventDefault() — stop default behavior", codeExample: "const btn = document.querySelector('#myButton');\n\nbtn.addEventListener('click', function(event) {\n  console.log('Clicked!', event.target);\n  event.target.textContent = 'Clicked!';\n});\n\n// Keyboard events\ndocument.addEventListener('keydown', (e) => {\n  console.log('Key pressed:', e.key);\n  if (e.key === 'Enter') {\n    console.log('Enter detected!');\n  }\n});", difficulty: "intermediate", category: "dom", order: 31, isCompleted: false },
    { id: "i12", title: "Fetch API and HTTP", description: "Making HTTP requests to fetch data from servers", content: "The Fetch API lets you make network requests to get or send data.\n\nHTTP methods:\n• GET — retrieve data\n• POST — send new data\n• PUT / PATCH — update data\n• DELETE — remove data\n\nResponse handling:\n• response.ok — check success\n• response.json() — parse JSON\n• response.text() — plain text", codeExample: "// GET request\nasync function getUser(id) {\n  const response = await fetch(`https://jsonplaceholder.typicode.com/users/${id}`);\n  if (!response.ok) throw new Error('Failed to fetch');\n  const user = await response.json();\n  console.log(user.name, user.email);\n}\n\n// POST request\nasync function createPost(data) {\n  const res = await fetch('https://jsonplaceholder.typicode.com/posts', {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify(data)\n  });\n  return res.json();\n}\n\ngetUser(1);", difficulty: "intermediate", category: "async", order: 32, isCompleted: false },
    { id: "i13", title: "JSON and Data Formats", description: "Working with JSON to exchange data", content: "JSON (JavaScript Object Notation) is the standard format for exchanging data between client and server.\n\nKey methods:\n• JSON.stringify() — convert JS object → JSON string\n• JSON.parse() — convert JSON string → JS object\n\nJSON rules:\n• Keys must be double-quoted strings\n• No functions or undefined values\n• Supports: strings, numbers, booleans, null, arrays, objects", codeExample: "const user = {\n  name: 'Alice',\n  age: 25,\n  hobbies: ['reading', 'coding']\n};\n\n// To JSON string\nconst jsonString = JSON.stringify(user);\nconsole.log(jsonString);\n\n// Pretty print\nconsole.log(JSON.stringify(user, null, 2));\n\n// Parse back\nconst parsed = JSON.parse(jsonString);\nconsole.log(parsed.name);      // Alice\nconsole.log(parsed.hobbies);  // ['reading', 'coding']", difficulty: "intermediate", category: "data", order: 33, isCompleted: false },
    { id: "i14", title: "localStorage and sessionStorage", description: "Storing data in the browser", content: "Web Storage lets you save data in the browser without a database.\n\nlocalStorage:\n• Persists forever (until cleared)\n• Available across tabs\n\nsessionStorage:\n• Clears when tab closes\n• Tab-specific\n\nMethods:\n• setItem(key, value)\n• getItem(key)\n• removeItem(key)\n• clear()\n\nAlways stringify objects before storing.", codeExample: "// Save data\nlocalStorage.setItem('username', 'Alice');\nlocalStorage.setItem('theme', 'dark');\n\n// Save objects\nconst prefs = { fontSize: 16, lang: 'en' };\nlocalStorage.setItem('prefs', JSON.stringify(prefs));\n\n// Read data\nconst username = localStorage.getItem('username');\nconsole.log(username); // Alice\n\nconst savedPrefs = JSON.parse(localStorage.getItem('prefs'));\nconsole.log(savedPrefs.fontSize); // 16\n\n// Remove\nlocalStorage.removeItem('theme');", difficulty: "intermediate", category: "browser", order: 34, isCompleted: false },
    { id: "i15", title: "Closures", description: "Functions that remember their surrounding scope", content: "A closure is a function that retains access to variables from its outer scope even after the outer function has finished.\n\nWhy closures matter:\n• Data privacy\n• Factory functions\n• Memoization\n• Event handlers with state\n\nEvery function in JavaScript is a closure.", codeExample: "// Counter using closure\nfunction makeCounter(start = 0) {\n  let count = start;\n  return {\n    increment() { count++; return count; },\n    decrement() { count--; return count; },\n    value() { return count; }\n  };\n}\n\nconst counter = makeCounter(10);\nconsole.log(counter.increment()); // 11\nconsole.log(counter.increment()); // 12\nconsole.log(counter.decrement()); // 11\nconsole.log(counter.value());     // 11", difficulty: "intermediate", category: "functions", order: 35, isCompleted: false },
    { id: "i16", title: "Prototype and Inheritance", description: "How JavaScript objects share behavior", content: "Every JavaScript object has a prototype — a hidden link to another object it inherits from.\n\nPrototype chain:\n• Object → prototype → Object.prototype → null\n• Property lookup walks up the chain\n\nModern inheritance uses classes (which are built on prototypes under the hood).", codeExample: "// Prototype-based\nfunction Vehicle(make, speed) {\n  this.make = make;\n  this.speed = speed;\n}\nVehicle.prototype.describe = function() {\n  return `${this.make} goes ${this.speed} km/h`;\n};\n\nconst car = new Vehicle('Toyota', 180);\nconsole.log(car.describe());\n\n// Check prototype chain\nconsole.log(car instanceof Vehicle); // true\nconsole.log(Object.getPrototypeOf(car) === Vehicle.prototype); // true", difficulty: "intermediate", category: "oop", order: 36, isCompleted: false },
    { id: "i17", title: "Functional Programming Basics", description: "Writing predictable, reusable code with pure functions", content: "Functional programming (FP) is a style that treats computation as evaluating functions.\n\nCore principles:\n• Pure functions — same input always gives same output, no side effects\n• Immutability — don't modify data, create new copies\n• Function composition — build complex behavior from simple functions\n• Higher-order functions — functions that accept/return functions", codeExample: "// Pure function\nconst add = (a, b) => a + b;\n\n// Immutability — don't mutate, transform\nconst original = [1, 2, 3];\nconst withFour = [...original, 4]; // new array\nconsole.log(original);   // [1, 2, 3] unchanged\nconsole.log(withFour);   // [1, 2, 3, 4]\n\n// Composition\nconst double = x => x * 2;\nconst addOne = x => x + 1;\nconst doubleThenAdd = x => addOne(double(x));\nconsole.log(doubleThenAdd(5)); // 11", difficulty: "intermediate", category: "functional", order: 37, isCompleted: false },
    { id: "i18", title: "Symbol and Iterators", description: "Custom symbols and making objects iterable", content: "Symbols are unique, immutable primitive values — great for unique keys.\n\nIterators define how an object is looped over with for...of.\n\nAn iterator must implement the Symbol.iterator method returning an object with a next() method.", codeExample: "// Symbol\nconst id = Symbol('userId');\nconst user = {\n  name: 'Alice',\n  [id]: 42\n};\nconsole.log(user[id]);   // 42\nconsole.log(user.name);  // Alice\n\n// Custom iterator\nconst range = {\n  from: 1,\n  to: 5,\n  [Symbol.iterator]() {\n    let current = this.from;\n    const last = this.to;\n    return {\n      next() {\n        return current <= last\n          ? { value: current++, done: false }\n          : { done: true };\n      }\n    };\n  }\n};\n\nfor (const n of range) console.log(n); // 1 2 3 4 5", difficulty: "intermediate", category: "es6", order: 38, isCompleted: false },
    { id: "i19", title: "Generators", description: "Functions that can pause and resume execution", content: "Generators are special functions that can yield multiple values one at a time.\n\nCreating generators:\n• Use function* syntax\n• yield pauses execution and returns a value\n• Calling next() resumes from where it left off\n\nUse cases: lazy sequences, infinite data streams, async control flow.", codeExample: "function* numberGenerator(start, end) {\n  for (let i = start; i <= end; i++) {\n    yield i;\n  }\n}\n\nconst gen = numberGenerator(1, 5);\nconsole.log(gen.next()); // { value: 1, done: false }\nconsole.log(gen.next()); // { value: 2, done: false }\n\n// Spread works too\nconsole.log([...numberGenerator(1, 5)]); // [1,2,3,4,5]\n\n// Infinite generator\nfunction* infiniteId() {\n  let id = 1;\n  while (true) yield id++;\n}\nconst ids = infiniteId();\nconsole.log(ids.next().value); // 1\nconsole.log(ids.next().value); // 2", difficulty: "intermediate", category: "es6", order: 39, isCompleted: false },
    { id: "i20", title: "WeakMap and WeakRef", description: "Memory-efficient data structures", content: "WeakMap and WeakSet hold weak references — the garbage collector can reclaim entries when nothing else holds a reference.\n\nWeakMap:\n• Keys must be objects\n• Not iterable (no size, no forEach)\n• Great for private data per object\n\nWeakRef:\n• Holds a weak reference to an object\n• Use .deref() to access the value (may be undefined)", codeExample: "// WeakMap for private data\nconst privateData = new WeakMap();\n\nclass User {\n  constructor(name, secret) {\n    this.name = name;\n    privateData.set(this, { secret });\n  }\n  getSecret() {\n    return privateData.get(this).secret;\n  }\n}\n\nconst u = new User('Alice', 'hunter2');\nconsole.log(u.name);       // Alice\nconsole.log(u.getSecret()); // hunter2\n// privateData is not directly accessible", difficulty: "intermediate", category: "data-structures", order: 40, isCompleted: false },

    // ── ADVANCED (20 lessons) ──────────────────────────────────────────
    { id: "a1", title: "The Event Loop Explained", description: "How JavaScript handles concurrency under the hood", content: "JavaScript is single-threaded but non-blocking. The event loop is what makes this possible.\n\nComponents:\n• Call Stack — executes synchronous code\n• Web APIs — handle setTimeout, fetch, DOM events\n• Callback Queue — holds ready callbacks\n• Microtask Queue — holds Promise callbacks (higher priority)\n\nOrder: Call Stack → Microtasks → Callback Queue", codeExample: "console.log('1 - Start');\n\nsetTimeout(() => console.log('4 - setTimeout'), 0);\n\nPromise.resolve()\n  .then(() => console.log('3 - Promise microtask'));\n\nconsole.log('2 - End');\n\n// Output order: 1, 2, 3, 4\n// Microtasks (Promise) run before macrotasks (setTimeout)\n\nasync function demo() {\n  console.log('A');\n  await Promise.resolve();\n  console.log('C - after await');\n}\ndemo();\nconsole.log('B');", difficulty: "advanced", category: "internals", order: 41, isCompleted: false },
    { id: "a2", title: "Closures Deep Dive", description: "Advanced patterns and real-world uses of closures", content: "Closures enable powerful patterns beyond simple counters.\n\nAdvanced uses:\n• Module pattern — private state\n• Memoization — cache expensive results\n• Partial application — pre-fill arguments\n• Currying — transform multi-arg functions\n\nUnderstanding closures is key to writing professional JavaScript.", codeExample: "// Memoization\nfunction memoize(fn) {\n  const cache = new Map();\n  return function(...args) {\n    const key = JSON.stringify(args);\n    if (cache.has(key)) return cache.get(key);\n    const result = fn.apply(this, args);\n    cache.set(key, result);\n    return result;\n  };\n}\n\nconst expensiveCalc = memoize((n) => {\n  console.log('Computing...');\n  return n * n;\n});\n\nconsole.log(expensiveCalc(5)); // Computing... 25\nconsole.log(expensiveCalc(5)); // 25 (from cache)\nconsole.log(expensiveCalc(6)); // Computing... 36", difficulty: "advanced", category: "functions", order: 42, isCompleted: false },
    { id: "a3", title: "Design Patterns: Creational", description: "Singleton, Factory, and Builder patterns", content: "Design patterns are proven solutions to common software problems.\n\nCreational patterns:\n• Singleton — only one instance exists\n• Factory — create objects without specifying exact class\n• Builder — construct complex objects step by step\n\nThese patterns improve code organization and maintainability.", codeExample: "// Singleton\nclass Database {\n  static #instance = null;\n  #connection;\n\n  constructor(url) {\n    if (Database.#instance) return Database.#instance;\n    this.#connection = `Connected to ${url}`;\n    Database.#instance = this;\n  }\n  query(sql) { return `${this.#connection}: ${sql}`; }\n}\n\nconst db1 = new Database('localhost:5432');\nconst db2 = new Database('remote:5432');\nconsole.log(db1 === db2); // true — same instance\nconsole.log(db1.query('SELECT *')); // Connected to localhost:5432: SELECT *", difficulty: "advanced", category: "patterns", order: 43, isCompleted: false },
    { id: "a4", title: "Design Patterns: Structural", description: "Observer, Decorator, and Proxy patterns", content: "Structural patterns deal with how objects are composed.\n\nPatterns covered:\n• Observer — subscribe to events (pub/sub)\n• Decorator — add behavior without modifying original\n• Proxy — intercept and control access to objects\n\nThese are widely used in frameworks like React and Vue.", codeExample: "// Observer pattern\nclass EventBus {\n  #listeners = new Map();\n\n  on(event, cb) {\n    if (!this.#listeners.has(event)) this.#listeners.set(event, []);\n    this.#listeners.get(event).push(cb);\n  }\n\n  emit(event, data) {\n    this.#listeners.get(event)?.forEach(cb => cb(data));\n  }\n}\n\nconst bus = new EventBus();\nbus.on('login', user => console.log('User logged in:', user.name));\nbus.on('login', user => console.log('Send welcome email to:', user.email));\nbus.emit('login', { name: 'Alice', email: 'alice@ex.com' });", difficulty: "advanced", category: "patterns", order: 44, isCompleted: false },
    { id: "a5", title: "Proxy and Reflect", description: "Intercepting and customizing object operations", content: "Proxy lets you intercept fundamental operations on objects.\n\nCommon traps:\n• get — intercept property reads\n• set — intercept property writes\n• has — intercept 'in' operator\n• deleteProperty — intercept delete\n\nReflect provides default implementations for all traps.\n\nUse cases: validation, logging, reactive systems (Vue 3 uses Proxy).", codeExample: "function createValidated(target, validators) {\n  return new Proxy(target, {\n    set(obj, prop, value) {\n      if (validators[prop] && !validators[prop](value)) {\n        throw new TypeError(`Invalid value for ${prop}: ${value}`);\n      }\n      return Reflect.set(obj, prop, value);\n    }\n  });\n}\n\nconst user = createValidated({}, {\n  age: v => typeof v === 'number' && v >= 0 && v <= 150,\n  name: v => typeof v === 'string' && v.length > 0\n});\n\nuser.name = 'Alice'; // OK\nuser.age = 25;       // OK\n// user.age = -5;   // TypeError!", difficulty: "advanced", category: "internals", order: 45, isCompleted: false },
    { id: "a6", title: "TypeScript Fundamentals", description: "Adding static types to JavaScript", content: "TypeScript is a typed superset of JavaScript that compiles to plain JS.\n\nKey features:\n• Static typing — catch errors before running\n• Interfaces — define object shapes\n• Generics — reusable typed components\n• Enums — named constant sets\n• Type inference — TS often figures out types automatically\n\nTypeScript is now industry standard for large applications.", codeExample: "// Basic types\nlet name: string = 'Alice';\nlet age: number = 25;\nlet active: boolean = true;\n\n// Interface\ninterface User {\n  id: number;\n  name: string;\n  email: string;\n  role?: 'admin' | 'user'; // optional, union type\n}\n\n// Generic function\nfunction first<T>(arr: T[]): T | undefined {\n  return arr[0];\n}\n\nconsole.log(first([1, 2, 3]));       // 1\nconsole.log(first(['a', 'b', 'c'])); // 'a'\n\n// Type narrowing\nfunction display(value: string | number) {\n  if (typeof value === 'string') {\n    console.log(value.toUpperCase());\n  } else {\n    console.log(value.toFixed(2));\n  }\n}", difficulty: "advanced", category: "typescript", order: 46, isCompleted: false },
    { id: "a7", title: "TypeScript Advanced Types", description: "Mapped types, conditionals, and utility types", content: "TypeScript's type system is extremely powerful.\n\nAdvanced features:\n• Union and Intersection types\n• Mapped types — transform existing types\n• Conditional types — types that depend on conditions\n• Utility types — Partial, Required, Pick, Omit, Record\n• Template literal types", codeExample: "interface User {\n  id: number;\n  name: string;\n  email: string;\n  password: string;\n}\n\n// Utility types\ntype PublicUser = Omit<User, 'password'>;\ntype PartialUser = Partial<User>;\ntype RequiredUser = Required<User>;\n\n// Mapped type\ntype Readonly<T> = { readonly [K in keyof T]: T[K] };\n\n// Conditional type\ntype IsString<T> = T extends string ? 'yes' : 'no';\ntype Test1 = IsString<string>; // 'yes'\ntype Test2 = IsString<number>; // 'no'\n\n// Record\ntype PageConfig = Record<'home' | 'about' | 'contact', { title: string }>;\nconst pages: PageConfig = {\n  home: { title: 'Home' },\n  about: { title: 'About' },\n  contact: { title: 'Contact' }\n};", difficulty: "advanced", category: "typescript", order: 47, isCompleted: false },
    { id: "a8", title: "Memory Management", description: "How JavaScript manages memory and avoiding leaks", content: "JavaScript uses automatic garbage collection, but you can still cause memory leaks.\n\nGarbage collection:\n• Mark-and-sweep algorithm\n• Removes objects with no references\n\nCommon leaks:\n• Global variables accumulating\n• Forgotten event listeners\n• Closures holding large objects\n• Detached DOM nodes\n\nTools: Chrome DevTools Memory profiler.", codeExample: "// Memory leak — event listener not removed\nfunction setupBadListener() {\n  const largeData = new Array(1000000).fill('data');\n  document.addEventListener('click', () => {\n    console.log(largeData.length); // largeData never freed\n  });\n}\n\n// Fix — remove listener when done\nfunction setupGoodListener() {\n  const largeData = new Array(1000).fill('data');\n  const handler = () => {\n    console.log(largeData.length);\n    document.removeEventListener('click', handler);\n  };\n  document.addEventListener('click', handler);\n}\n\n// Use WeakMap for object-linked data\nconst cache = new WeakMap();\nfunction process(obj) {\n  if (!cache.has(obj)) cache.set(obj, expensiveCompute(obj));\n  return cache.get(obj);\n}", difficulty: "advanced", category: "performance", order: 48, isCompleted: false },
    { id: "a9", title: "Performance Optimization", description: "Techniques for writing fast JavaScript", content: "Performance matters for user experience. Key techniques:\n\n• Avoid layout thrashing — batch DOM reads/writes\n• Debounce and throttle — limit event handler frequency\n• Lazy loading — load resources only when needed\n• Virtual scrolling — render only visible items\n• Web Workers — offload heavy computation\n• Caching — store computed results", codeExample: "// Debounce — wait until user stops typing\nfunction debounce(fn, delay) {\n  let timer;\n  return function(...args) {\n    clearTimeout(timer);\n    timer = setTimeout(() => fn.apply(this, args), delay);\n  };\n}\n\nconst search = debounce((query) => {\n  console.log('Searching for:', query);\n}, 300);\n\n// Throttle — run at most once per interval\nfunction throttle(fn, interval) {\n  let lastRun = 0;\n  return function(...args) {\n    const now = Date.now();\n    if (now - lastRun >= interval) {\n      lastRun = now;\n      fn.apply(this, args);\n    }\n  };\n}\n\nconst onScroll = throttle(() => console.log('scroll'), 100);", difficulty: "advanced", category: "performance", order: 49, isCompleted: false },
    { id: "a10", title: "Web Workers", description: "Running JavaScript in background threads", content: "Web Workers run scripts in background threads separate from the main thread.\n\nBenefits:\n• Heavy computation doesn't freeze the UI\n• True parallelism for CPU-intensive tasks\n\nLimitations:\n• No DOM access\n• Communicate via messages\n\nUse cases: image processing, data analysis, crypto operations.", codeExample: "// worker.js\nself.addEventListener('message', (e) => {\n  const { numbers } = e.data;\n  const result = numbers.reduce((sum, n) => sum + n, 0);\n  self.postMessage({ sum: result });\n});\n\n// main.js\nconst worker = new Worker('worker.js');\nconst bigArray = Array.from({ length: 1000000 }, (_, i) => i);\n\nworker.postMessage({ numbers: bigArray });\n\nworker.addEventListener('message', (e) => {\n  console.log('Sum:', e.data.sum);\n  worker.terminate(); // clean up\n});\n\nconsole.log('UI still responsive while worker computes!');", difficulty: "advanced", category: "browser", order: 50, isCompleted: false },
    { id: "a11", title: "Service Workers and PWA", description: "Offline capabilities and progressive web apps", content: "Service Workers are scripts that run between your app and the network.\n\nCapabilities:\n• Intercept network requests\n• Cache assets for offline use\n• Push notifications\n• Background sync\n\nPWA requirements:\n• HTTPS\n• Web App Manifest\n• Service Worker\n\nPWAs install like native apps and work offline.", codeExample: "// Register service worker\nif ('serviceWorker' in navigator) {\n  navigator.serviceWorker.register('/sw.js')\n    .then(reg => console.log('SW registered:', reg.scope))\n    .catch(err => console.error('SW failed:', err));\n}\n\n// sw.js — cache on install\nconst CACHE = 'v1';\nconst ASSETS = ['/', '/index.html', '/app.js', '/styles.css'];\n\nself.addEventListener('install', e => {\n  e.waitUntil(\n    caches.open(CACHE).then(cache => cache.addAll(ASSETS))\n  );\n});\n\nself.addEventListener('fetch', e => {\n  e.respondWith(\n    caches.match(e.request).then(cached => cached || fetch(e.request))\n  );\n});", difficulty: "advanced", category: "browser", order: 51, isCompleted: false },
    { id: "a12", title: "Testing with Jest", description: "Writing and running unit tests", content: "Testing ensures your code works correctly and prevents regressions.\n\nJest concepts:\n• describe() — group related tests\n• test() / it() — individual test case\n• expect() — assertion\n• beforeEach / afterEach — setup and teardown\n• Mock functions — spy on calls\n\nTesting approaches:\n• Unit tests — test single function\n• Integration tests — test how parts work together", codeExample: "// math.js\nexport function add(a, b) { return a + b; }\nexport function divide(a, b) {\n  if (b === 0) throw new Error('Division by zero');\n  return a / b;\n}\n\n// math.test.js\nimport { add, divide } from './math';\n\ndescribe('Math utilities', () => {\n  test('adds two numbers', () => {\n    expect(add(2, 3)).toBe(5);\n    expect(add(-1, 1)).toBe(0);\n  });\n\n  test('divides correctly', () => {\n    expect(divide(10, 2)).toBe(5);\n  });\n\n  test('throws on division by zero', () => {\n    expect(() => divide(5, 0)).toThrow('Division by zero');\n  });\n});", difficulty: "advanced", category: "testing", order: 52, isCompleted: false },
    { id: "a13", title: "Reactive Programming", description: "Streams and observables with RxJS", content: "Reactive programming treats data as streams of events over time.\n\nRxJS key concepts:\n• Observable — a stream of values\n• Observer — subscribes and reacts to values\n• Operators — transform streams (map, filter, merge)\n• Subject — both Observable and Observer\n\nGreat for: real-time data, user input, WebSocket streams.", codeExample: "import { Observable, Subject, interval, fromEvent } from 'rxjs';\nimport { map, filter, debounceTime, take } from 'rxjs/operators';\n\n// Create an observable\nconst numbers$ = interval(1000).pipe(\n  take(5),\n  map(n => n * 2)\n);\n\nnumbers$.subscribe({\n  next: val => console.log('Value:', val),\n  complete: () => console.log('Done!')\n});\n\n// From DOM event\nconst clicks$ = fromEvent(document, 'click').pipe(\n  map(e => ({ x: e.clientX, y: e.clientY }))\n);\n\nclicks$.subscribe(pos => console.log('Clicked at:', pos));", difficulty: "advanced", category: "patterns", order: 53, isCompleted: false },
    { id: "a14", title: "Canvas API", description: "Drawing graphics with the HTML5 Canvas", content: "The Canvas API lets you draw 2D graphics directly in the browser.\n\nKey operations:\n• getContext('2d') — get drawing context\n• fillRect / strokeRect — rectangles\n• arc — circles\n• beginPath / lineTo — custom paths\n• fillText — text rendering\n• drawImage — render images\n\nCanvas powers games, charts, and image editing tools.", codeExample: "const canvas = document.getElementById('myCanvas');\nconst ctx = canvas.getContext('2d');\n\n// Background\nctx.fillStyle = '#1a1a2e';\nctx.fillRect(0, 0, canvas.width, canvas.height);\n\n// Circle\nctx.beginPath();\nctx.arc(150, 150, 80, 0, Math.PI * 2);\nctx.fillStyle = '#e94560';\nctx.fill();\n\n// Text\nctx.font = 'bold 24px Arial';\nctx.fillStyle = 'white';\nctx.textAlign = 'center';\nctx.fillText('Canvas!', 150, 160);\n\n// Animate\nfunction animate() {\n  requestAnimationFrame(animate);\n  ctx.clearRect(0, 0, canvas.width, canvas.height);\n  // redraw...\n}", difficulty: "advanced", category: "browser", order: 54, isCompleted: false },
    { id: "a15", title: "WebSockets", description: "Real-time bidirectional communication", content: "WebSockets provide a persistent, full-duplex connection between client and server.\n\nVs HTTP:\n• HTTP — request/response (client initiates)\n• WebSocket — persistent, either side can send\n\nUse cases: chat apps, live dashboards, multiplayer games, stock tickers.\n\nEvents:\n• open — connection established\n• message — data received\n• error — connection error\n• close — connection closed", codeExample: "// Client-side\nconst ws = new WebSocket('wss://echo.websocket.org');\n\nws.addEventListener('open', () => {\n  console.log('Connected!');\n  ws.send(JSON.stringify({ type: 'hello', text: 'Hi server!' }));\n});\n\nws.addEventListener('message', (event) => {\n  const data = JSON.parse(event.data);\n  console.log('Received:', data);\n});\n\nws.addEventListener('close', () => {\n  console.log('Disconnected');\n});\n\n// Send a message\nfunction sendMessage(text) {\n  if (ws.readyState === WebSocket.OPEN) {\n    ws.send(JSON.stringify({ type: 'message', text }));\n  }\n}", difficulty: "advanced", category: "networking", order: 55, isCompleted: false },
    { id: "a16", title: "GraphQL Basics", description: "Flexible API queries with GraphQL", content: "GraphQL is a query language for APIs — ask for exactly what you need.\n\nVs REST:\n• REST — multiple endpoints, fixed responses\n• GraphQL — single endpoint, you define the shape\n\nCore concepts:\n• Schema — defines types and relationships\n• Query — fetch data\n• Mutation — modify data\n• Subscription — real-time updates\n• Resolver — function that returns data for a field", codeExample: "// GraphQL Query (client-side)\nconst query = `\n  query GetUser($id: ID!) {\n    user(id: $id) {\n      name\n      email\n      posts {\n        title\n        createdAt\n      }\n    }\n  }\n`;\n\nasync function fetchUser(id) {\n  const res = await fetch('/graphql', {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query,\n      variables: { id }\n    })\n  });\n  const { data } = await res.json();\n  return data.user;\n}\n\nfetchUser('1').then(console.log);", difficulty: "advanced", category: "networking", order: 56, isCompleted: false },
    { id: "a17", title: "Security Fundamentals", description: "XSS, CSRF, and secure coding practices", content: "Web security is critical. Common vulnerabilities:\n\n• XSS (Cross-Site Scripting) — injecting malicious scripts into pages\n• CSRF (Cross-Site Request Forgery) — tricking users into unwanted actions\n• SQL Injection — malicious database queries\n• Insecure Direct Object References\n\nDefenses:\n• Sanitize/escape user input\n• Use Content Security Policy\n• CSRF tokens for forms\n• HTTPS everywhere\n• Validate on both client and server", codeExample: "// XSS Prevention — NEVER do this:\n// element.innerHTML = userInput; // DANGEROUS!\n\n// DO this instead:\nfunction safeSetText(element, text) {\n  element.textContent = text; // auto-escapes HTML\n}\n\n// Or sanitize with DOMPurify library:\n// element.innerHTML = DOMPurify.sanitize(userInput);\n\n// CSRF protection — include token in requests\nasync function postWithCSRF(url, data) {\n  const token = document.querySelector('meta[name=csrf-token]').content;\n  return fetch(url, {\n    method: 'POST',\n    headers: {\n      'Content-Type': 'application/json',\n      'X-CSRF-Token': token\n    },\n    body: JSON.stringify(data)\n  });\n}", difficulty: "advanced", category: "security", order: 57, isCompleted: false },
    { id: "a18", title: "Node.js and Server-Side JS", description: "Building server applications with Node.js", content: "Node.js lets you run JavaScript on the server.\n\nKey features:\n• Non-blocking I/O — handles many requests without waiting\n• npm ecosystem — millions of packages\n• Event-driven architecture\n• Built-in modules: fs, http, path, crypto, os\n\nCommon use cases:\n• REST APIs\n• Real-time servers\n• Command-line tools\n• Microservices", codeExample: "// Simple HTTP server\nconst http = require('http');\nconst fs = require('fs').promises;\n\nconst server = http.createServer(async (req, res) => {\n  if (req.url === '/') {\n    res.writeHead(200, { 'Content-Type': 'text/html' });\n    res.end('<h1>Hello from Node.js!</h1>');\n  } else if (req.url === '/api/time') {\n    res.writeHead(200, { 'Content-Type': 'application/json' });\n    res.end(JSON.stringify({ time: new Date().toISOString() }));\n  } else {\n    res.writeHead(404);\n    res.end('Not Found');\n  }\n});\n\nserver.listen(3000, () => console.log('Server on port 3000'));", difficulty: "advanced", category: "backend", order: 58, isCompleted: false },
    { id: "a19", title: "Code Splitting and Lazy Loading", description: "Loading code only when it is needed", content: "Code splitting reduces initial bundle size by loading code on demand.\n\nTechniques:\n• Dynamic import() — load modules when needed\n• React.lazy() — lazy-load React components\n• Route-based splitting — load page code when navigating\n\nBenefits:\n• Faster initial page load\n• Better Time to Interactive\n• Reduced bandwidth", codeExample: "// Dynamic import\nconst button = document.getElementById('loadChart');\n\nbutton.addEventListener('click', async () => {\n  // Only loads Chart.js when user clicks\n  const { Chart } = await import('chart.js');\n  const ctx = document.getElementById('canvas');\n  new Chart(ctx, { type: 'bar', data: { /* ... */ } });\n});\n\n// React lazy loading\nimport React, { lazy, Suspense } from 'react';\n\nconst Dashboard = lazy(() => import('./Dashboard'));\nconst Analytics = lazy(() => import('./Analytics'));\n\nfunction App() {\n  return (\n    <Suspense fallback={<div>Loading...</div>}>\n      <Dashboard />\n    </Suspense>\n  );\n}", difficulty: "advanced", category: "performance", order: 59, isCompleted: false },
    { id: "a20", title: "Meta-programming", description: "Writing code that writes or modifies code", content: "Meta-programming means writing code that can inspect or modify other code at runtime.\n\nJavaScript meta-programming tools:\n• Proxy & Reflect — intercept object operations\n• Symbol.toPrimitive — customize type conversion\n• Object.defineProperty — control property behavior\n• Decorators (Stage 3) — annotate classes and methods\n• eval / Function() — dynamic code execution (use carefully)", codeExample: "// Computed property names\nconst field = 'username';\nconst obj = { [field]: 'Alice' };\nconsole.log(obj.username); // Alice\n\n// Symbol.toPrimitive\nclass Money {\n  constructor(amount, currency) {\n    this.amount = amount;\n    this.currency = currency;\n  }\n  [Symbol.toPrimitive](hint) {\n    if (hint === 'number') return this.amount;\n    if (hint === 'string') return `${this.amount} ${this.currency}`;\n    return this.amount;\n  }\n}\n\nconst price = new Money(29.99, 'USD');\nconsole.log(`Price: ${price}`);   // Price: 29.99 USD\nconsole.log(price + 10);         // 39.99\nconsole.log(price > 20);        // true", difficulty: "advanced", category: "internals", order: 60, isCompleted: false },

    // ── EXPERT (20 lessons) ────────────────────────────────────────────
    { id: "e1", title: "V8 Engine Internals", description: "How Chrome's JavaScript engine works", content: "V8 is Google's open-source JavaScript engine used in Chrome and Node.js.\n\nPipeline:\n1. Parsing — source → AST\n2. Ignition (interpreter) — AST → bytecode\n3. TurboFan (compiler) — hot bytecode → optimized machine code\n4. Deoptimization — fall back if assumptions break\n\nOptimization tips:\n• Keep object shapes consistent (hidden classes)\n• Avoid polymorphic functions\n• Use typed arrays for numeric data", codeExample: "// GOOD — consistent object shape (V8 creates one hidden class)\nfunction createPoint(x, y) {\n  return { x, y }; // always same shape\n}\n\n// BAD — different shapes confuse V8\nfunction createUser(name, age, role) {\n  const user = { name };\n  if (age) user.age = age;   // sometimes has age\n  if (role) user.role = role; // sometimes has role\n  return user;\n}\n\n// Typed arrays — much faster for number crunching\nconst floats = new Float64Array(1000000);\nfor (let i = 0; i < floats.length; i++) floats[i] = i * 1.5;\n\nconsole.time('sum');\nlet sum = 0;\nfor (let i = 0; i < floats.length; i++) sum += floats[i];\nconsole.timeEnd('sum');", difficulty: "expert", category: "internals", order: 61, isCompleted: false },
    { id: "e2", title: "Advanced TypeScript Patterns", description: "Template literal types, infer, and type-level programming", content: "TypeScript's type system is Turing complete — you can do complex logic purely at the type level.\n\nAdvanced features:\n• Template literal types — string manipulation at type level\n• infer — extract types from conditional types\n• Variadic tuple types — typed spread in tuples\n• Recursive types — self-referential type definitions\n• Type predicates — custom type guards", codeExample: "// Template literal types\ntype EventName<T extends string> = `on${Capitalize<T>}`;\ntype ClickEvent = EventName<'click'>; // 'onClick'\n\n// infer\ntype ReturnType<T> = T extends (...args: any[]) => infer R ? R : never;\ntype Fn = () => Promise<string>;\ntype Result = ReturnType<Fn>; // Promise<string>\n\n// Recursive type\ntype DeepReadonly<T> = {\n  readonly [K in keyof T]: T[K] extends object ? DeepReadonly<T[K]> : T[K];\n};\n\n// Type predicate\nfunction isUser(obj: unknown): obj is { name: string; email: string } {\n  return typeof obj === 'object' && obj !== null &&\n    'name' in obj && 'email' in obj;\n}", difficulty: "expert", category: "typescript", order: 62, isCompleted: false },
    { id: "e3", title: "Compiler Design Basics", description: "Building a simple interpreter or transpiler", content: "Compilers transform source code into another form. Understanding compiler stages helps you write better tools (linters, transpilers, code formatters).\n\nStages:\n1. Lexing / Tokenization — source → tokens\n2. Parsing — tokens → AST\n3. Semantic Analysis — validate meaning\n4. Code Generation — AST → output\n\nJavaScript tooling is built on these concepts: Babel, ESLint, TypeScript, Prettier.", codeExample: "// Mini expression lexer\nfunction tokenize(input) {\n  const tokens = [];\n  let i = 0;\n  while (i < input.length) {\n    if (/\\s/.test(input[i])) { i++; continue; }\n    if (/\\d/.test(input[i])) {\n      let num = '';\n      while (i < input.length && /\\d/.test(input[i])) num += input[i++];\n      tokens.push({ type: 'NUMBER', value: Number(num) });\n    } else if ('+-*/'.includes(input[i])) {\n      tokens.push({ type: 'OP', value: input[i++] });\n    } else { throw new Error(`Unknown char: ${input[i]}`); }\n  }\n  return tokens;\n}\n\nconsole.log(tokenize('12 + 34 * 5'));\n// [{type:'NUMBER',value:12},{type:'OP',value:'+'},{type:'NUMBER',value:34},{type:'OP',value:'*'},{type:'NUMBER',value:5}]", difficulty: "expert", category: "internals", order: 63, isCompleted: false },
    { id: "e4", title: "Monads and Functional Patterns", description: "Category theory concepts applied to JavaScript", content: "Monads are a design pattern from category theory used to handle effects (errors, async, nullability) in a composable way.\n\nCommon monads in JS:\n• Maybe / Option — handle null safely\n• Result / Either — handle errors without try/catch\n• Promise — async computation chain\n• Array (flatMap) — non-determinism\n\nCore operations: unit (wrap) and flatMap (chain).", codeExample: "// Maybe monad\nclass Maybe {\n  constructor(value) { this._value = value; }\n  static of(value) { return new Maybe(value); }\n  isNothing() { return this._value == null; }\n  map(fn) {\n    return this.isNothing() ? this : Maybe.of(fn(this._value));\n  }\n  flatMap(fn) {\n    return this.isNothing() ? this : fn(this._value);\n  }\n  getOrElse(defaultVal) {\n    return this.isNothing() ? defaultVal : this._value;\n  }\n}\n\nconst user = { address: { city: 'NYC' } };\nconst city = Maybe.of(user)\n  .map(u => u.address)\n  .map(a => a.city)\n  .getOrElse('Unknown');\nconsole.log(city); // NYC\n\nconst city2 = Maybe.of(null)\n  .map(u => u.address)\n  .getOrElse('Unknown');\nconsole.log(city2); // Unknown", difficulty: "expert", category: "functional", order: 64, isCompleted: false },
    { id: "e5", title: "Microservices Architecture", description: "Designing distributed systems with Node.js", content: "Microservices split a large application into small, independent services.\n\nCore principles:\n• Single responsibility — each service does one thing\n• Independent deployment\n• Own data store per service\n• Communication via HTTP, gRPC, or message queues\n\nChallenges:\n• Network latency\n• Distributed transactions\n• Service discovery\n• Observability (logging, tracing, metrics)", codeExample: "// API Gateway pattern\nconst express = require('express');\nconst { createProxyMiddleware } = require('http-proxy-middleware');\n\nconst gateway = express();\n\n// Route to microservices\ngateway.use('/api/users', createProxyMiddleware({\n  target: 'http://user-service:3001',\n  changeOrigin: true\n}));\n\ngateway.use('/api/orders', createProxyMiddleware({\n  target: 'http://order-service:3002',\n  changeOrigin: true\n}));\n\ngateway.use('/api/products', createProxyMiddleware({\n  target: 'http://product-service:3003',\n  changeOrigin: true\n}));\n\ngateway.listen(8080, () => console.log('Gateway on :8080'));", difficulty: "expert", category: "architecture", order: 65, isCompleted: false },
    { id: "e6", title: "WebAssembly (WASM)", description: "Running near-native code in the browser", content: "WebAssembly is a binary instruction format that runs in browsers at near-native speed.\n\nKey facts:\n• Not a replacement for JavaScript — they work together\n• Compiled from C, C++, Rust, Go, AssemblyScript\n• Sandboxed — safe execution\n• Runs in the same JS engine\n\nUse cases: game engines, image processing, video editing, cryptography, CAD tools.", codeExample: "// Instantiate a WASM module (from compiled C/Rust)\nasync function loadWasm() {\n  const response = await fetch('math.wasm');\n  const buffer = await response.arrayBuffer();\n  const { instance } = await WebAssembly.instantiate(buffer, {\n    env: {\n      // JS functions callable from WASM\n      log: (n) => console.log('From WASM:', n)\n    }\n  });\n\n  // Call exported WASM functions\n  const { add, fibonacci } = instance.exports;\n  console.log(add(10, 20));        // 30\n  console.log(fibonacci(40));     // extremely fast vs JS\n}\n\n// AssemblyScript (TypeScript-like WASM source)\n// export function add(a: i32, b: i32): i32 { return a + b; }\n\nloadWasm();", difficulty: "expert", category: "performance", order: 66, isCompleted: false },
    { id: "e7", title: "Advanced System Design", description: "Designing scalable, resilient JavaScript systems", content: "System design at scale requires thinking beyond code.\n\nKey patterns:\n• Circuit Breaker — fail fast when a service is down\n• Rate Limiting — protect APIs from abuse\n• CQRS — separate reads and writes\n• Event Sourcing — store events, not state\n• Saga Pattern — distributed transaction coordination\n\nScalability strategies:\n• Horizontal scaling\n• Caching layers (Redis)\n• CDN for static assets\n• Database sharding and read replicas", codeExample: "// Circuit Breaker implementation\nclass CircuitBreaker {\n  #state = 'CLOSED';\n  #failures = 0;\n  #threshold;\n  #timeout;\n  #nextAttempt = Date.now();\n\n  constructor(threshold = 5, timeout = 60000) {\n    this.#threshold = threshold;\n    this.#timeout = timeout;\n  }\n\n  async call(fn) {\n    if (this.#state === 'OPEN') {\n      if (Date.now() < this.#nextAttempt) throw new Error('Circuit OPEN');\n      this.#state = 'HALF-OPEN';\n    }\n    try {\n      const result = await fn();\n      this.#onSuccess();\n      return result;\n    } catch (err) {\n      this.#onFailure();\n      throw err;\n    }\n  }\n\n  #onSuccess() { this.#failures = 0; this.#state = 'CLOSED'; }\n  #onFailure() {\n    this.#failures++;\n    if (this.#failures >= this.#threshold) {\n      this.#state = 'OPEN';\n      this.#nextAttempt = Date.now() + this.#timeout;\n    }\n  }\n}", difficulty: "expert", category: "architecture", order: 67, isCompleted: false },
    { id: "e8", title: "Custom Framework Design", description: "Build your own mini JavaScript framework", content: "Understanding how frameworks work makes you a better developer.\n\nThis lesson builds a minimal reactive UI framework:\n• Virtual DOM — lightweight representation of the real DOM\n• Diffing algorithm — find minimal changes\n• Reactive state — re-render on change\n• Component model — reusable UI units\n\nThis is the foundation of React, Vue, and Svelte.", codeExample: "// Mini reactive framework\nfunction createState(initial) {\n  let state = initial;\n  const subscribers = new Set();\n  return {\n    get: () => state,\n    set: (newState) => {\n      state = typeof newState === 'function' ? newState(state) : newState;\n      subscribers.forEach(fn => fn(state));\n    },\n    subscribe: (fn) => {\n      subscribers.add(fn);\n      return () => subscribers.delete(fn);\n    }\n  };\n}\n\n// Usage\nconst counter = createState(0);\ncounter.subscribe(val => {\n  document.getElementById('count').textContent = val;\n});\n\ndocument.getElementById('inc').addEventListener('click', () => {\n  counter.set(n => n + 1);\n});\n\nconsole.log(counter.get()); // 0", difficulty: "expert", category: "architecture", order: 68, isCompleted: false },
    { id: "e9", title: "Cryptography in JavaScript", description: "Hashing, encryption, and the Web Crypto API", content: "The Web Crypto API provides cryptographic operations in the browser and Node.js.\n\nOperations available:\n• digest — hashing (SHA-256, etc.)\n• generateKey — create crypto keys\n• encrypt / decrypt — AES-GCM, RSA-OAEP\n• sign / verify — digital signatures\n• deriveKey — from password (PBKDF2)\n\nAlways use built-in crypto — never roll your own!", codeExample: "// SHA-256 hash\nasync function sha256(message) {\n  const encoder = new TextEncoder();\n  const data = encoder.encode(message);\n  const hashBuffer = await crypto.subtle.digest('SHA-256', data);\n  const hashArray = Array.from(new Uint8Array(hashBuffer));\n  return hashArray.map(b => b.toString(16).padStart(2, '0')).join('');\n}\n\n// AES-GCM encryption\nasync function encryptMessage(key, message) {\n  const enc = new TextEncoder();\n  const iv = crypto.getRandomValues(new Uint8Array(12));\n  const encrypted = await crypto.subtle.encrypt(\n    { name: 'AES-GCM', iv },\n    key,\n    enc.encode(message)\n  );\n  return { encrypted, iv };\n}\n\nsha256('Hello, World!').then(console.log);", difficulty: "expert", category: "security", order: 69, isCompleted: false },
    { id: "e10", title: "Database Architecture and Optimization", description: "Advanced database patterns for JavaScript applications", content: "Database design decisions have huge impact on application performance.\n\nKey patterns:\n• N+1 Query Problem — and how to solve it with joins/DataLoader\n• Connection pooling — reuse DB connections\n• Database indexing strategy\n• Optimistic vs pessimistic locking\n• Read replicas for scaling\n• CQRS with separate read/write models\n\nNode.js database tools: Drizzle ORM, Prisma, Knex, pg.", codeExample: "// N+1 problem — BAD\nasync function getBlogPostsBad(db) {\n  const posts = await db.query('SELECT * FROM posts');\n  for (const post of posts) {\n    // N extra queries for N posts!\n    post.author = await db.query('SELECT * FROM users WHERE id = $1', [post.userId]);\n  }\n  return posts;\n}\n\n// Solution — JOIN in one query\nasync function getBlogPostsGood(db) {\n  return db.query(`\n    SELECT p.*, u.name as authorName, u.email as authorEmail\n    FROM posts p\n    JOIN users u ON p.user_id = u.id\n    ORDER BY p.created_at DESC\n  `);\n}\n\n// Or use DataLoader (batching)\nconst userLoader = new DataLoader(async (ids) => {\n  const users = await db.query('SELECT * FROM users WHERE id = ANY($1)', [ids]);\n  return ids.map(id => users.find(u => u.id === id));\n});", difficulty: "expert", category: "backend", order: 70, isCompleted: false },
    { id: "e11", title: "Observability and Monitoring", description: "Logging, tracing, and metrics in production", content: "Observability means understanding what your system is doing through its outputs.\n\nThree pillars:\n• Logs — recorded events with context\n• Metrics — numerical measurements over time (latency, error rate)\n• Traces — request flow across services\n\nTools:\n• Winston / Pino — structured logging\n• Prometheus / Grafana — metrics\n• OpenTelemetry — distributed tracing\n• Sentry — error tracking", codeExample: "const pino = require('pino');\n\nconst logger = pino({\n  level: 'info',\n  transport: {\n    target: 'pino-pretty',\n    options: { colorize: true }\n  }\n});\n\n// Structured logging — always log context\nfunction handleRequest(req, res) {\n  const start = Date.now();\n  const requestId = crypto.randomUUID();\n\n  logger.info({ requestId, method: req.method, url: req.url }, 'Request started');\n\n  res.on('finish', () => {\n    const duration = Date.now() - start;\n    logger.info(\n      { requestId, status: res.statusCode, duration },\n      'Request completed'\n    );\n  });\n}\n\n// Never log sensitive data\n// WRONG: logger.info({ password: user.password });\n// RIGHT: logger.info({ userId: user.id });", difficulty: "expert", category: "devops", order: 71, isCompleted: false },
    { id: "e12", title: "CI/CD for JavaScript Projects", description: "Automating testing and deployment pipelines", content: "CI/CD (Continuous Integration / Continuous Deployment) automates the path from code to production.\n\nCI — on every push:\n• Run linters\n• Run tests\n• Check types\n• Build the project\n\nCD — on merge to main:\n• Deploy to staging\n• Run integration tests\n• Deploy to production\n• Notify team\n\nTools: GitHub Actions, GitLab CI, CircleCI, Jenkins.", codeExample: "# .github/workflows/ci.yml\nname: CI\non: [push, pull_request]\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: '20'\n          cache: 'npm'\n      - run: npm ci\n      - run: npm run lint\n      - run: npm run typecheck\n      - run: npm test -- --coverage\n      - run: npm run build\n\n  deploy:\n    needs: test\n    if: github.ref == 'refs/heads/main'\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - run: npm ci && npm run build\n      - name: Deploy to production\n        run: ./deploy.sh\n        env:\n          DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}", difficulty: "expert", category: "devops", order: 72, isCompleted: false },
    { id: "e13", title: "Monorepo Architecture", description: "Managing multiple packages in one repository", content: "Monorepos store multiple related projects in a single repository.\n\nBenefits:\n• Atomic cross-package changes\n• Shared tooling and dependencies\n• Easier code sharing\n\nTools:\n• npm / pnpm workspaces — dependency linking\n• Turborepo — task caching and parallelism\n• Nx — smart build system\n• Changesets — versioning and changelogs\n\nUsed by: Google, Meta, Microsoft, Vercel.", codeExample: "# pnpm-workspace.yaml\npackages:\n  - 'apps/*'\n  - 'packages/*'\n\n# Directory structure:\n# apps/\n#   web/          — Next.js app\n#   mobile/       — Expo app\n#   api/          — Express server\n# packages/\n#   ui/           — shared components\n#   utils/        — shared utilities\n#   types/        — shared TypeScript types\n\n// packages/utils/src/index.ts\nexport function formatDate(date: Date): string {\n  return date.toLocaleDateString('en-US', {\n    year: 'numeric', month: 'long', day: 'numeric'\n  });\n}\n\n// apps/web/src/page.tsx\nimport { formatDate } from '@myorg/utils';\nconsole.log(formatDate(new Date())); // shared!", difficulty: "expert", category: "architecture", order: 73, isCompleted: false },
    { id: "e14", title: "Event Sourcing and CQRS", description: "Advanced data modeling with event-driven architecture", content: "Event Sourcing stores the history of all changes as events, not just current state.\n\nVs traditional:\n• Traditional — store current state (UPDATE users SET name='Alice')\n• Event Sourcing — store events (UserNameChanged: Alice)\n\nBenefits:\n• Full audit history\n• Time travel debugging\n• Replay events to rebuild state\n\nCQRS (Command Query Responsibility Segregation):\n• Commands — write side, changes state\n• Queries — read side, reads state\n• Separate models for reading and writing", codeExample: "// Event store\nclass EventStore {\n  #events = [];\n\n  append(aggregateId, event) {\n    this.#events.push({\n      aggregateId,\n      ...event,\n      timestamp: Date.now(),\n      version: this.#events.filter(e => e.aggregateId === aggregateId).length\n    });\n  }\n\n  getEvents(aggregateId) {\n    return this.#events.filter(e => e.aggregateId === aggregateId);\n  }\n}\n\n// Rebuild state from events\nfunction replayAccount(events) {\n  return events.reduce((state, event) => {\n    switch (event.type) {\n      case 'ACCOUNT_CREATED': return { ...state, balance: 0, owner: event.owner };\n      case 'MONEY_DEPOSITED': return { ...state, balance: state.balance + event.amount };\n      case 'MONEY_WITHDRAWN': return { ...state, balance: state.balance - event.amount };\n      default: return state;\n    }\n  }, {});\n}", difficulty: "expert", category: "architecture", order: 74, isCompleted: false },
    { id: "e15", title: "Advanced Testing Strategies", description: "Property-based testing, mutation testing, and more", content: "Beyond basic unit tests, advanced testing strategies find deeper bugs.\n\nStrategies:\n• Property-based testing — test with random inputs, not just examples\n• Mutation testing — verify tests actually catch bugs\n• Contract testing — verify API contracts between services\n• Snapshot testing — detect unintended UI changes\n• Performance testing — load and stress testing\n\nTools: fast-check (property-based), Stryker (mutation), Pact (contract).", codeExample: "import * as fc from 'fast-check';\n\n// Property-based test — runs hundreds of random inputs\ndescribe('sort', () => {\n  test('sorted array always has same length', () => {\n    fc.assert(fc.property(\n      fc.array(fc.integer()),\n      (arr) => {\n        const sorted = [...arr].sort((a, b) => a - b);\n        return sorted.length === arr.length;\n      }\n    ));\n  });\n\n  test('each element is <= the next', () => {\n    fc.assert(fc.property(\n      fc.array(fc.integer()),\n      (arr) => {\n        const sorted = [...arr].sort((a, b) => a - b);\n        return sorted.every((v, i) => i === 0 || sorted[i-1] <= v);\n      }\n    ));\n  });\n});", difficulty: "expert", category: "testing", order: 75, isCompleted: false },
    { id: "e16", title: "Language Server Protocol", description: "Building developer tooling and IDE extensions", content: "The Language Server Protocol (LSP) standardizes how editors communicate with language tools.\n\nLSP powers:\n• Autocomplete\n• Go-to-definition\n• Find references\n• Diagnostics / linting\n• Code actions\n\nBuilding your own language server lets you create custom developer tools for any language or DSL.", codeExample: "import {\n  createConnection,\n  TextDocuments,\n  Diagnostic,\n  DiagnosticSeverity,\n  ProposedFeatures\n} from 'vscode-languageserver/node';\nimport { TextDocument } from 'vscode-languageserver-textdocument';\n\nconst connection = createConnection(ProposedFeatures.all);\nconst documents = new TextDocuments(TextDocument);\n\ndocuments.onDidChangeContent(({ document }) => {\n  const text = document.getText();\n  const diagnostics: Diagnostic[] = [];\n\n  // Custom rule: warn on console.log\n  const pattern = /console\\.log/g;\n  let match;\n  while ((match = pattern.exec(text))) {\n    const pos = document.positionAt(match.index);\n    diagnostics.push({\n      severity: DiagnosticSeverity.Warning,\n      range: { start: pos, end: { line: pos.line, character: pos.character + match[0].length } },\n      message: 'Remove console.log before committing',\n      source: 'my-linter'\n    });\n  }\n  connection.sendDiagnostics({ uri: document.uri, diagnostics });\n});\n\ndocuments.listen(connection);\nconnection.listen();", difficulty: "expert", category: "tooling", order: 76, isCompleted: false },
    { id: "e17", title: "Distributed Caching Strategies", description: "Redis, cache invalidation, and caching patterns", content: "Caching dramatically improves performance and reduces database load.\n\nCaching patterns:\n• Cache-Aside — app manages cache manually\n• Write-Through — write to cache and DB together\n• Write-Behind — write to cache, async to DB\n• Read-Through — cache handles DB reads\n\nCache invalidation strategies:\n• TTL (Time-To-Live)\n• Event-based invalidation\n• Cache versioning\n\nTools: Redis, Memcached, Cloudflare KV.", codeExample: "const redis = require('redis');\nconst client = redis.createClient();\n\n// Cache-aside pattern\nasync function getUserWithCache(userId) {\n  const cacheKey = `user:${userId}`;\n\n  // 1. Check cache\n  const cached = await client.get(cacheKey);\n  if (cached) {\n    console.log('Cache HIT');\n    return JSON.parse(cached);\n  }\n\n  // 2. Cache miss — fetch from DB\n  console.log('Cache MISS');\n  const user = await db.query('SELECT * FROM users WHERE id = $1', [userId]);\n\n  // 3. Store in cache with 1-hour TTL\n  await client.setEx(cacheKey, 3600, JSON.stringify(user));\n\n  return user;\n}\n\n// Invalidate cache on update\nasync function updateUser(userId, data) {\n  await db.query('UPDATE users SET ... WHERE id = $1', [userId]);\n  await client.del(`user:${userId}`); // bust cache\n}", difficulty: "expert", category: "backend", order: 77, isCompleted: false },
    { id: "e18", title: "Real-Time Architecture at Scale", description: "Building systems for millions of concurrent users", content: "Real-time features (chat, live feeds, collaborative editing) at scale require careful architecture.\n\nKey challenges:\n• Connection state — stateless HTTP vs stateful WebSocket\n• Pub/Sub at scale — fan-out to millions of subscribers\n• Presence — knowing who is online\n• Conflict resolution — concurrent edits (CRDT)\n\nTools:\n• Redis Pub/Sub — message broadcasting\n• Apache Kafka — high-throughput event streaming\n• Socket.IO — WebSocket abstraction\n• Ably, Pusher — managed real-time", codeExample: "const { Server } = require('socket.io');\nconst { createAdapter } = require('@socket.io/redis-adapter');\nconst { createClient } = require('redis');\n\n// Redis adapter enables horizontal scaling\n// Multiple server instances share state via Redis\nasync function setupRealtime(httpServer) {\n  const pubClient = createClient({ url: process.env.REDIS_URL });\n  const subClient = pubClient.duplicate();\n  await Promise.all([pubClient.connect(), subClient.connect()]);\n\n  const io = new Server(httpServer);\n  io.adapter(createAdapter(pubClient, subClient));\n\n  io.on('connection', (socket) => {\n    socket.on('join-room', (roomId) => {\n      socket.join(roomId);\n      socket.to(roomId).emit('user-joined', socket.id);\n    });\n\n    socket.on('message', (roomId, text) => {\n      io.to(roomId).emit('message', { from: socket.id, text });\n    });\n  });\n}\n", difficulty: "expert", category: "architecture", order: 78, isCompleted: false },
    { id: "e19", title: "Machine Learning in JavaScript", description: "TensorFlow.js and running ML models in the browser", content: "TensorFlow.js brings machine learning to JavaScript — in the browser and Node.js.\n\nCapabilities:\n• Run pre-trained models (image classification, NLP)\n• Transfer learning — fine-tune existing models\n• Train models from scratch\n• WebGL GPU acceleration in browser\n\nCommon models:\n• MobileNet — image classification\n• COCO-SSD — object detection\n• BlazeFace — face detection\n• USE — universal sentence encoder", codeExample: "import * as tf from '@tensorflow/tfjs';\nimport * as mobilenet from '@tensorflow-models/mobilenet';\n\nasync function classifyImage(imageElement) {\n  // Load pre-trained model\n  const model = await mobilenet.load();\n\n  // Classify — returns top predictions\n  const predictions = await model.classify(imageElement);\n  predictions.forEach(p => {\n    console.log(`${p.className}: ${(p.probability * 100).toFixed(1)}%`);\n  });\n  return predictions;\n}\n\n// Train a simple model\nconst model = tf.sequential({\n  layers: [\n    tf.layers.dense({ inputShape: [2], units: 4, activation: 'relu' }),\n    tf.layers.dense({ units: 1, activation: 'sigmoid' })\n  ]\n});\n\nmodel.compile({ optimizer: 'adam', loss: 'binaryCrossentropy', metrics: ['accuracy'] });\n\n// XOR dataset\nconst xs = tf.tensor2d([[0,0],[0,1],[1,0],[1,1]]);\nconst ys = tf.tensor2d([[0],[1],[1],[0]]);\nawait model.fit(xs, ys, { epochs: 500 });\nconsole.log(model.predict(tf.tensor2d([[0,1]])).dataSync());", difficulty: "expert", category: "ai", order: 79, isCompleted: false },
    { id: "e20", title: "Open Source Contribution Mastery", description: "Reading, contributing to, and maintaining large codebases", content: "Contributing to open source is the fastest way to reach expert level.\n\nHow to contribute:\n1. Find an issue — good-first-issue label\n2. Fork and clone the repo\n3. Set up development environment\n4. Write a failing test first (TDD)\n5. Implement the fix\n6. Run full test suite\n7. Write clear commit messages\n8. Open a pull request with description\n\nMaintaining:\n• Code review best practices\n• Semantic versioning (semver)\n• Writing changelogs\n• Community communication", codeExample: "// Conventional Commits — standard commit message format\n// <type>(<scope>): <description>\n//\n// feat: add new feature\n// fix: bug fix\n// docs: documentation only\n// style: formatting, no logic change\n// refactor: no new feature or fix\n// perf: performance improvement\n// test: adding tests\n// chore: build/tooling changes\n\n// Examples:\n// feat(auth): add OAuth2 login with Google\n// fix(parser): handle empty string input\n// docs(readme): update installation instructions\n// perf(sort): use Timsort for better average case\n\n// Semantic Versioning: MAJOR.MINOR.PATCH\n// 1.0.0 → 1.0.1 (patch: bug fix)\n// 1.0.0 → 1.1.0 (minor: new feature, backwards compatible)\n// 1.0.0 → 2.0.0 (major: breaking change)\n\n// package.json version ranges:\n// '^1.2.3' — compatible with 1.x.x\n// '~1.2.3' — compatible with 1.2.x\n// '1.2.3'  — exactly this version", difficulty: "expert", category: "fundamentals", order: 80, isCompleted: false },
  ]).onConflictDoNothing();

  await db.insert(challenges).values([
    {
      id: "1",
      title: "Array Manipulation",
      description: "Find the second largest number in an array without sorting",
      difficulty: "medium",
      category: "arrays",
      testCases: [
        { input: [1, 3, 4, 5, 2], expected: 4 },
        { input: [10, 20, 30], expected: 20 },
      ],
      solution: "function findSecondLargest(arr) {\n  let first = arr[0], second = -1;\n  for (let i = 1; i < arr.length; i++) {\n    if (arr[i] > first) { second = first; first = arr[i]; }\n    else if (arr[i] > second && arr[i] < first) { second = arr[i]; }\n  }\n  return second;\n}",
      hints: ["Think about tracking two variables", "Don't sort the array"],
    },
    {
      id: "2",
      title: "String Reversal",
      description: "Reverse a string without using the built-in reverse method",
      difficulty: "easy",
      category: "strings",
      testCases: [
        { input: "hello", expected: "olleh" },
        { input: "JavaScript", expected: "tpircSavaJ" },
      ],
      solution: "function reverseString(str) {\n  let result = '';\n  for (let i = str.length - 1; i >= 0; i--) {\n    result += str[i];\n  }\n  return result;\n}",
      hints: ["Loop backwards through the string", "Build a new string character by character"],
    },
    {
      id: "3",
      title: "FizzBuzz",
      description: "Print numbers 1-100 with Fizz for multiples of 3, Buzz for 5, FizzBuzz for both",
      difficulty: "easy",
      category: "logic",
      testCases: [
        { input: 15, expected: "FizzBuzz" },
        { input: 9, expected: "Fizz" },
        { input: 10, expected: "Buzz" },
      ],
      solution: "function fizzBuzz(n) {\n  if (n % 15 === 0) return 'FizzBuzz';\n  if (n % 3 === 0) return 'Fizz';\n  if (n % 5 === 0) return 'Buzz';\n  return String(n);\n}",
      hints: ["Check divisibility by 15 first", "Use the modulo operator %"],
    },
  ]).onConflictDoNothing();

  await db.insert(communityPosts).values([
    { id: "1", userId: "sample-user", title: "Can someone explain the difference between let and const?", content: "I'm having trouble understanding when to use let vs const in JavaScript.", category: "questions", likes: 5, replies: 3 },
    { id: "2", userId: "sample-user-2", title: "Just completed my first JavaScript project!", content: "Thanks to the AI tutor for the helpful explanations.", category: "achievements", likes: 12, replies: 1 },
  ]).onConflictDoNothing();

  console.log("✅ Database seeded successfully");
}
